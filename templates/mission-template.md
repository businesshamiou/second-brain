---
type: mission
mission_id: "NNN"            # permanent identity of the lineage, a quoted string (e.g. "018")
correction:                  # "Cxx" if a correction, then `supersedes:` (next line) carries the path of the replaced file; otherwise omit both keys entirely (no empty value)
supersedes:
status: AUTHORIZED           # authorization at creation — frozen, never touched afterwards
title: "<Mission title>"
description: "<one or two sentences>"  # written by the author at deposit time, never generated by a third-party model (232341 §2.1)
created_at: "YYYY-MM-DDTHH:MM:SS±HH:MM"
timezone: America/Montreal
scope: <slug-kebab-case>
artifact_state: GENERATED_IN_CHAT
---

# MISSION <NNN> — <TITLE>

Reminder: `status` above freezes the authorization at the creation of this file; it is never touched again. The execution state of this Mission lives exclusively in `<projet>/missions/MISSION-INDEX.md`, column « Statut » [status]. This template is filled in with the `ecriture-de-mission` skill.

## Measured existing state

<!-- Mandatory as soon as the Scope includes the creation of a file (DECISION-2026-09-04-121443 point 3). Table: listing or search command run, output, explicit conclusion — "no file fulfils this function" or "file X fulfils it partially, the Mission amends it instead of creating". A Mission that creates without this rubric filled in is refused. The parameters of a third-party instrument (caps, quotas, key limits, `max_tokens`) enter here on the same footing as a corpus file (report 157, F9). -->

| Command | Result kept |
|---|---|
| <command run> | <what it showed> |

<Conclusion: what is created, what is amended, and why.>

## Context

<!-- Mandatory (DECISION-2026-09-01-115547 point 1). In this order: the measured facts that motivate the Mission, each dated and qualified `MESURÉ` [measured] / `DECLARED` / `HYPOTHÈSE` [hypothesis]; what the Executor will find on disk and why it is there; the known pitfalls (precedents, tool defects, misleading locations); why the decision that authorizes this Mission is what it is. -->

<Measured facts, expected state on disk, pitfalls, reason for the decision.>

## Objective

<Result pursued and reason for opening this Mission.>

## Scope

<Repositories, folders or systems concerned.>

<!-- As soon as a step regenerates an index, the `superseded-files.txt` of the repository concerned enters the Scope: it is a guaranteed output of the indexing tool, never an exception (report 132, case C4). -->

Out of scope: <what is explicitly excluded.>

## Preconditions

<!-- What the Executor measures before writing anything at all; at the slightest non-trivial gap: STOP and report, no writing (232341, Impact). -->
<!-- A git status tolerance already named by the previous Mission is not carried over: a batch 0 that settles it, or a cited Owner arbitration (checklist, line 26). -->

1. <Expected state, measured before any writing; non-trivial gap = STOP.>
2. <When the Mission changes a file of the Vault: `bash tests/run-suite.sh --changed origin/main` green, before any writing. Never a hand-made list of reference tests (Mission 209, Decision 235129): the runner plays the two guardian lines and the tests the changed files call for.>

## Sources

- <Other source: report, Decision, earlier measurement.>

## Applicable decisions

- <Decision or cross-cutting constraint that applies to this Mission.>

## Constraints

- <Limit to respect during execution.>

## Prior measurements

<!-- Mandatory as soon as a step creates, copies, installs, pins, wires or configures anything at all (report 157, F6). One row per target laid down: what is going to be laid down, the command that measures the state of the target before laying it down, and what it returned. Laying down a target that has no row here is laying down blind: eight out of twenty-six were on Missions 145-156, and five ended in a stop or in null work. -->

| Target | Command run | Result |
|---|---|---|
| <what step N will lay down> | <command that measures the state of the target> | <what it returned> |

Any expected behaviour of a tool, a hook or a flag that has not been exercised by a command is written `HYPOTHÈSE` [hypothesis] in the Context.
<!-- Commit simulation (checklist, line 24): a "## Commit simulation" section follows this rubric as soon as a commit is prescribed — per commit, staged files, guardians run, expected verdict and its reason, hook configuration as it will be after the Mission. -->

## Fan-out

<!-- OPTIONAL (« Fan-out » [parallel read-only batches]). Leave the rubric out, or leave it empty, and the Mission is valid and the Executor works as before; it is never required and never a reason to refuse a Mission. Fill it only for measurements that do not depend on one another: one batch = one folder or one set of targets that can be measured without waiting for another batch. Each batch names its targets, its read commands and its output file under the Executor's temporary folder. The batches are READ-ONLY: a sub-agent measures and writes its own output file, nothing else; no commit, no push, no move. Only the Executor writes to a repository, gathers the output files and cites each one in the report with the batch that produced it. Eligibility, sequential fallback and "delegation does not widen the authority of the task": the skill `dispatching-parallel-agents` (not delivered to the Vault yet). -->

| Batch | Targets | Measures | Output |
|---|---|---|---|
| <B1> | <targets measurable on their own> | <read commands> | <output file under the Executor's temporary folder> |

## Steps

<!-- No "delete" step: either "move to _trash/" (agent step, fingerprint + absence remeasured in validation), or "deletion by the Owner" as a human gate outside the steps, measured at the resume (DECISION-2026-08-29-110852). -->
0. **Preflight** (DECISION-2026-09-04-154756, point 1) — mandatory as soon as the Scope includes the commit of an artifact deposited by the Pilot. Run on the files of the Scope all the applicable checks, collect **all** the violations, report them in one go, stop if any remains. The preflight corrects nothing: a defect is corrected by the role that wrote the file.
<!-- As soon as a Vault commit is prescribed: explicit batch "Vault commit → stop → Owner push → re-pinning of (workshop history, not distributed) → continuation" (checklist, line 25). The Resume contract does not declare "healthy" a state in which (workshop history, not distributed) can no longer commit. -->
<!-- As soon as a step modifies an index guardian (freshness, weight): rerun the Vault's tools/bench-guardians.sh bench before and after, table pasted into the report (Mission 164). -->
1. <Ordered step.>

## Gates

- Owner authorization: <granted for ... | pending>
- Human gate not granted: <what remains outside the authorization>
- Stop conditions: <signal that interrupts the Mission>

## Validations

<!-- Cross-review done by the Pilot before deposit (DECISION-2026-09-01-115547 point 2): each criterion below is reachable without violating a prohibition of Gates or Constraints; each step is allowed by the same prohibitions. -->
<!-- Each criterion carries a relative count in figures, before and after (232341, Impact): not "fixed", but "3 before, 0 after". -->

- <Verifiable criterion that confirms the result, with its before/after count in figures.>

## Exit contract

<Form of the expected closing report: VERIFIED/ANOMALY distinction, proofs, SHA of the commits, final remeasurement, explicit stop.>

## Resume contract

<!-- Filled in only if the window stops at PARTIEL: last healthy commit, what remains to be done, how the next window resumes without replaying what is done (232341, Impact). -->

<Last healthy commit; remaining steps; resume point for the next window.>

## Doors

<!-- One line per door that this Mission opens or closes, with its exact CLOSE: line if it closes here (Decision 110935, 232341 Impact). -->

- <door key> — <CLOSE: ... | stays open, reason>

## Liens

- `prescribed by` — [Versioning of Missions and generated outputs](../rules/RULES-2026-08-17-211522-mission-versioning-and-generated-output.md)
- `amended by` — [Decision — Permanent deletion is an Owner gesture](../decisions/DECISION-2026-08-29-110852-deletion-is-owner-gesture-trash-zone.md)
- `amended by` — Decision — Internal consistency of Missions (workshop history, not distributed) (hors Vault)
- (to be completed: type — title — relative path, see the linking standard)
