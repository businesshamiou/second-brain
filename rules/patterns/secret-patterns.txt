# Motifs de detection de secrets — un motif par ligne, syntaxe grep -E.
# Les lignes vides et celles commencant par # sont ignorees.
# Ce fichier est une donnee : il s amende sans toucher au script.

# --- Prefixes de fournisseurs connus ---
AIza[0-9A-Za-z_-]{10,}
sk-[0-9A-Za-z_-]{16,}
ghp_[0-9A-Za-z]{20,}
gho_[0-9A-Za-z]{20,}
github_pat_[0-9A-Za-z_]{20,}
xox[baprs]-[0-9A-Za-z-]{10,}
AKIA[0-9A-Z]{12,}
-----BEGIN [A-Z ]*PRIVATE KEY-----

# --- Affectations de valeurs sensibles ---
# Un mot-cle sensible suivi de = ou : puis d une chaine longue.
(api[_-]?key|apikey|secret|token|password|passwd|bearer)["' ]*[=:]["' ]*[A-Za-z0-9_./+-]{16,}

# --- Chaines de connexion ---
[a-zA-Z][a-zA-Z0-9+.-]*://[^/\s:@]+:[^/\s:@]+@
