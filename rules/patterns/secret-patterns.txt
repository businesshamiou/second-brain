# Secret detection patterns — one pattern per line, grep -E syntax.
# Empty lines and those starting with # are ignored.
# This file is data: it is amended without touching the script.

# --- Known provider prefixes ---
AIza[0-9A-Za-z_-]{10,}
sk-[0-9A-Za-z_-]{16,}
ghp_[0-9A-Za-z]{20,}
gho_[0-9A-Za-z]{20,}
github_pat_[0-9A-Za-z_]{20,}
xox[baprs]-[0-9A-Za-z-]{10,}
AKIA[0-9A-Z]{12,}
-----BEGIN [A-Z ]*PRIVATE KEY-----

# --- Assignments of sensitive values ---
# A sensitive keyword followed by = or : then by a long string.
(api[_-]?key|apikey|secret|token|password|passwd|bearer)["' ]*[=:]["' ]*[A-Za-z0-9_./+-]{16,}

# --- Connection strings ---
[a-zA-Z][a-zA-Z0-9+.-]*://[^/\s:@]+:[^/\s:@]+@
